import React from 'react';

const breadcrumb = props => {
    console.log('props', props)
    return (
        <div>
            this is place holder for bearcrumb
        </div>
    )
}

export default breadcrumb;